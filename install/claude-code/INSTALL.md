# Claude Code

Copy or clone the complete repository to `~/.claude/skills/econfigure/`. Claude Code discovers `SKILL.md` from that directory and may invoke it automatically when the request matches its description. It can also be invoked as `/econfigure`.

For a project-only installation, place the complete directory at `<project>/.claude/skills/econfigure/`.
