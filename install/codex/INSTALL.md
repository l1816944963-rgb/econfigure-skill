# Codex

Copy the complete repository to `.agents/skills/econfigure/` in your working project. For a personal installation use `~/.agents/skills/econfigure/`.

Keep `SKILL.md`, `agents/openai.yaml`, and every supporting directory together. Invoke it as `$econfigure` or describe an applied-economics thesis figure task that matches the skill description.

See the [official Codex skills documentation](https://developers.openai.com/codex/skills/) for discovery paths supported by your client.
